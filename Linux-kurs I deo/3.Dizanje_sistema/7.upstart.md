3.Upstart

       sudo initctl list
       sudo initctl start ssh 
               sudo start ssh
       sudo initctl stop ssh
               sudo stop ssh
       sudo initctl restart ssh 
               sudo restart ssh
       sudo initctl status ssh 